// PostCSS configuration for Docusaurus
// This is a placeholder file created as part of the project structure
module.exports = {
  plugins: [
    require('postcss-import'),
    require('autoprefixer'),
  ],
};